# Dependencies and build output
node_modules/
dist/
dist-test/
**/node_modules/
**/dist/
**/dist-test/

# Symlinked duplicates of xx-stack/ — formatting them would rewrite the same
# physical files twice under a second path.
opencode-orchestration/mcp-server/
opencode-orchestration/scripts/
opencode-orchestration/packs/

# Content packs: design systems, skills, and rule books are largely vendored
# from upstream projects. Attribution and license texts live at the pack root
# (packs/rules/{LICENSE,manifest.json}, packs/design/{licenses/,manifest.json}),
# not per file — the vendored files themselves carry no license headers.
# Reformatting them would create churn in third-party content and destroy the
# byte-comparison against upstream that the manifests depend on.
packs/

# Generated artifacts
**/DESIGN-CATALOG.md
mcp-server/dist/
mcp-server/dist-test/

# Runtime state
hermes-orchestration/logs/

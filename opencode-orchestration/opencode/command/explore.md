---
description: Read-only map of the codebase or a blast radius
agent: research
---

Do not edit files. Do not implement.

If `build_repo_map` is available, call it first for an unfamiliar tree. Then read and grep until every factual claim has a file:line. Return the research output format: question, sources, findings, gaps.

Load `research-deep` only when the question needs a budgeted search/read/reason loop rather than a single pass.

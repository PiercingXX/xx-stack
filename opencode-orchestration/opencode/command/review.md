---
description: Production-grade pre-merge review of the current diff
agent: reviewer
---

Load the `review-code` skill, then review the current uncommitted and staged changes against the repo's default base.

Pin the baseline to a real commit first. Classify every finding S0–S3. Do not edit files. End with PASS, FAIL, or AMBIGUOUS and the evidence.
